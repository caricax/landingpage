steps:
  # Build da aplicação Angular
  - name: 'node:20-alpine'
    entrypoint: 'sh'
    args:
      - '-c'
      - |
        npm ci
        npm run build
    id: 'build-app'

  # Build da imagem Docker
  - name: 'gcr.io/cloud-builders/docker'
    args: 
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/caricax-landing:$BUILD_ID'
      - '-t'
      - 'gcr.io/$PROJECT_ID/caricax-landing:latest'
      - '.'
    id: 'build-image'
    waitFor: ['build-app']

  # Push da imagem para Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: 
      - 'push'
      - 'gcr.io/$PROJECT_ID/caricax-landing:latest'
    id: 'push-image'
    waitFor: ['build-image']

  # Deploy no Cloud Run
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'caricax-landing'
      - '--image'
      - 'gcr.io/$PROJECT_ID/caricax-landing:latest'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '8080'
      - '--memory'
      - '1Gi'
      - '--cpu'
      - '1'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '10'
      - '--set-env-vars'
      - 'NODE_ENV=production'
    id: 'deploy-cloudrun'
    waitFor: ['push-image']

# Configurações de timeout e logging
options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'

timeout: '1200s'

images:
  - 'gcr.io/$PROJECT_ID/caricax-landing:latest'
